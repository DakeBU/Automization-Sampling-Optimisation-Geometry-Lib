# Same-operator weighted distributional resolvent

Frozen packet continuing the accepted ordinary-gradient bridge. Same finite-dimensional real inner-product Borel space, C1 W, integrable exp(-W), normalized Gibbs mu. D is any given closable partial operator with exactly the compact smooth gradient graph. For epsilon>0 and f in scalar L2(mu), construct u in SAME D.closure.domain using ClosedGraphResolvent. Retain all-domain abstract weak equation, local volume integrability and ordinary weak derivative of u with G=D.closure u. For each smooth compact psi, prove all THREE weighted volume products integrable and epsilon integral exp(-W)u psi + integral exp(-W)<G,gradient psi> = integral exp(-W)f psi.

1. Reuse the actual closed-graph resolvent, then WeightedGradientDistribution on its same solution/gradient.
2. Compact smooth psi and gradient psi are L2(mu); construct their Lp classes via MemLp.toLp.
3. The exact original graph and its inclusion into the closure embed this test in the same domain, with image equal to its actual gradient class.
4. Test the retained abstract equation. Use L2.integrable_inner and representative ae equalities to prove all three mu-integrabilities and rewrite the L2 pairings as integrals.
5. integrable_tilted_iff gives all three weighted volume integrabilities; integral_tilted and positive-normalization cancellation give the divergence-form equation.

Use genuine smooth tests only: exp(W)psi need not be smooth under C1/C2 W and is not silently inserted into the original core. No H2, converse graph characterization, nondivergence regularity, D*D core, Poincare, measurable selection or paper result. Positive epsilon is only for the genuine resolvent existence parent. Local representative identity is already proved, not restated as an assumption. Focused test must obtain the real D from compact_gradient_closable and consume this existence theorem, not supply an arbitrary weak equation.

Source refresh 2026-09-13: PBPS C.1 uses smooth compact density/closedness and conditional Poincare; background1310.2526v7 sections2.4-2.5 discusses weighted Poisson and Sobolev spaces on compact manifolds. This direct Euclidean prerequisite supplies only a justified weak PDE for the existing route; no source regularity theorem is claimed.

Focused compile first PASS3035. The only trial adjustment removed an unnecessary tactic sequence-focus linter warning; final focused PASS3035, standard3 axioms, no warning. Independent complete mathematical/API and six-step lesson review PASS. The test actually constructs D before all epsilon and forcing choices; production constructs u from the existing resolvent theorem. A separately chosen ConditionalResolvent witness is not definitionally the same u: any such identification requires the retained all-domain uniqueness theorem. Source and fixed-commit admission remain separate.

Bounded next-route review (resolvent_distribution_review): before any L2 interior elliptic regularity, derive local square integrability for every Gibbs L2 representative a, independently of D/PDE. Target LocallyIntegrable (norm a)^2 and MemLp a 2 (volume.restrict K) for every compact K, with only continuous W and integrable exp(-W). Exact fixed APIs: memLp_two_iff_integrable_sq_norm; integrable_tilted_iff; continuous inverse-weight local multiplication; LocallyIntegrable.integrableOn_isCompact; measurability transfer via absolutelyContinuous_tilted and AEStronglyMeasurable.mono_ac. Applied to u,G,f this supplies actual H1_loc/L2_loc input. This is planning, not an H2 result or assumed Sobolev domain characterization. Keep the PDE in divergence form until a valid additional test-extension argument exists.
