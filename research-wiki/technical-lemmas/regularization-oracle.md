# Counted adaptive regularization oracle

SAU ANDI-OPT-regularization-oracle-001: generic finite deterministic execution plus
actual quadratic first-order simulation. Query count is accumulated only on the
oracle branch; next inspects halting even at zero fuel. none means exhaustion.
Program/initial state must precede f for a future class-uniform theorem.
No arbitrary returned-cost labels, random/unbounded equivalence, runtime or
full Lemma4.2 claim. Reuses first-order gradient parent; no duplicate calculus.
Conceptual mirror audit: none-found; this is an exact local reply simulation,
not a new cross-domain mechanism/transport.
Focused checks, independent audits and aggregate/publication gates pending.
Next: quantify a uniform solver program and true halting budget, then compose
regularized minimizer/radius/accuracy and positive-regime parameters.
